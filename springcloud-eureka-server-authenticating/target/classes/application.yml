server:
  port: 1111
security:
  basic:
    enabled: true
  user:
    name: test
    password: test
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone:  http://test:test@localhost:1111/eureka  # 安全的注册地址  
    
